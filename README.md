# Roumaissa Mekhoukh — Junction 2026 Portfolio

Replace `YOUR_EMAIL` and `YOUR_LINKEDIN_URL` in `index.html` before submitting. GitHub is already linked to `https://github.com/RoumaissaMkh`.

For GitHub Pages, keep `index.html`, `style.css`, and `script.js` in the repository root and deploy from `main` / `(root)`.
